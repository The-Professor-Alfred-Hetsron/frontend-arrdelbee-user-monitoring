export default function MonitoringLayout({
    children,
  }: {
    children: React.ReactNode
  }){
    return(
        <div className="">
            {/* Put your Monitoring Common components here */}
            {children}
        </div>
    )
  }